ap  gprs测试方法：

1.AP通过调试串口提供指令  gprssend200 ：每次下发该指令，AP会向指定链接的TCP服务器发送200字节的数据。
2.AP会转发tcp服务器下发的符合包头的N1数据，并向调试串口转发命令 gprs:receive data N

基于以上的AP提供的接口：

1.设备向服务器发送测试：
	发送1000包测试，通过调试串口发送命令，开始计时，收到服务器ack，记录刚才所用时间，没有收到ack，超时发送下一包，记录超时时间，直到发送完1000包，输出每次发送的时间。
	
2.服务器向设备发送测试：
	同上。